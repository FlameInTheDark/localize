Detect language of user message and write the language code in ISO 639-1 format. Output only language code (e.g. en, fr).
If text contains more than one language, output them with comma separator codes (e.g. en,fr).